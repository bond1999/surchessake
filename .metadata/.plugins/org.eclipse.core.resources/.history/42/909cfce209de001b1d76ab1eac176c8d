package piece;

import java.util.ArrayList;

public class pawn extends main.Piece {

	ArrayList<Integer> legalMoves = new ArrayList<Integer>();
	
	public void updateLegalMoves(ArrayList<main.Piece> pieceArray) {
		
		// UP
		if(main.chessboard.getPieceIndex(super.getLocation() + up) != 69) {
			if(main.chessboard.getPieceIndex(super.getLocation() + up) == -1) {
				
			}
				
		}

		// UP TWO
		
		// UP LEFT
		
		// UP RIGHT
			
	}
	
	
	
}
